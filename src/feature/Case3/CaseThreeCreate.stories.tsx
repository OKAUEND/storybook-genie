import type { Meta, StoryObj } from "@storybook/react";
import { CaseThree } from "./CaseThree";

type Story = StoryObj<typeof CaseThree>;

const meta: Meta = {
  title: "CaseThree",
  component: CaseThree,
};

export default meta;

export const Template: Story = {};
