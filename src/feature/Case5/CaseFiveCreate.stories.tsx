import type { Meta, StoryObj } from "@storybook/react";
import { CaseFive } from "./CaseFive";

type Story = StoryObj<typeof CaseFive>;

const meta: Meta = {
  title: "CaseFive",
  component: CaseFive,
};

export default meta;

export const Template: Story = {};
